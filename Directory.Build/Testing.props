<Project>
  <PropertyGroup>
    <PackageReferences>@(PackageReference)</PackageReferences>
    <IsTestProject>@(PackageReference->AnyHaveMetadataValue("Identity", "Microsoft.NET.Test.Sdk"))</IsTestProject>
  </PropertyGroup>

  <PropertyGroup>
    <Testing_props>imported</Testing_props>
  </PropertyGroup>
</Project>
